<?php
/**
 * إعدادات لوحة متابعة المتاجر — حِرف
 * ------------------------------------------------------------
 * 1) غيّر AUTH_KEY إلى نص سري طويل، واجعله مطابقًا تمامًا
 *    لقيمة AUTH_KEY في أعلى ملف index.html.
 *    ⚠️ استخدم حروفًا وأرقامًا لاتينية فقط (بدون حروف عربية) —
 *    لأن المفتاح يُرسل في ترويسة HTTP لا تقبل سوى Latin-1.
 * 2) DB_PATH هو مسار ملف قاعدة بيانات SQLite (يُنشأ تلقائيًا).
 *    احرص أن يكون مجلد data/ قابلًا للكتابة على الخادم.
 */

// 🔐 نفس المفتاح الموجود في index.html (غيّره قبل النشر — لاتيني فقط)
define('AUTH_KEY', 'change-this-to-a-long-secret-2026');

// 📦 مسار قاعدة البيانات (SQLite) — يُنشأ تلقائيًا عند أول تشغيل
define('DB_PATH', __DIR__ . '/data/store.sqlite');
